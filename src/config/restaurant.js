/**
 * Frontend fallback configuration for restaurant details.
 * Update RESTAURANT_FALLBACK_NAME to change the name shown when the API
 * (using VITE_RESTAURENT_ID) does not return a restaurant profile.
 */

export const RESTAURANT_FALLBACK_NAME = 'Five star';
export const RESTAURANT_LOGO_SRC = '/logo_res.png';

export default {
  RESTAURANT_FALLBACK_NAME,
  RESTAURANT_LOGO_SRC,
};

